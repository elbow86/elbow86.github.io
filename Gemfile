source "https://rubygems.org"

# Use the github-pages gem so GitHub Pages' build environment
# can satisfy dependencies. Do not pin `jekyll` directly here
# unless you intend to build the site with Actions.
gem "github-pages", group: :jekyll_plugins
